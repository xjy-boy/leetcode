#include<iostream>
#include<vector>
using namespace std;

class Solution {
public:
    int numbers[501];
    int checkWays(vector<vector<int>>& pairs) {
        memset(numbers, 0, sizeof(int) * 501);
        for (int i = 0; i < pairs.size(); i++) {

        }
    }

    bool isTree(vector<int> parents) {

    }
};