#include<iostream>
#include<vector>
#include<stack>
using namespace std;

class Solution {
public:
    long long subArrayRanges(vector<int>& nums) {
        vector<int> minIndex  = vector<int>(nums.size(), -1);
        vector<int> maxIndex  = vector<int>(nums.size(), -1);
        stack<int> greaterStack;
    }
};
